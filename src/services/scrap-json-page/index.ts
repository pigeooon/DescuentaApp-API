import { IBank, IBankCategory } from "../../interfaces/bank.interface";
import { IDiscount } from "../../interfaces/discount.interface";

export const scrapJsonPage = async (bank: IBank, bankCategory: IBankCategory): Promise<IDiscount[]> => {
    return [];
}